# Django settings
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=change-me-to-a-strong-secret
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Postgres settings
POSTGRES_DB=orgdb
POSTGRES_USER=orguser
POSTGRES_PASSWORD=orgpassword
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Branding (used in printable invoice/receipt headers)
# Optional: leave empty to omit in printouts
BRAND_NAME=OrgMS
# Path under static/ e.g., logo.png (place file in ./static/)
BRAND_LOGO_FILE=
BRAND_ADDRESS=123 Example Street, City, Country
BRAND_PHONE=+1-555-0100
BRAND_EMAIL=billing@example.com
